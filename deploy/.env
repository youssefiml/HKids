# Required for JWT signing in API and parent auth flows.
# Use at least 32 characters for production deployments.
JWT_SECRET=replace_with_a_strong_secret_key_min_32_chars

# Optional overrides
JWT_EXPIRES_IN=7d
PARENT_JWT_EXPIRES_IN=12h
FRONTEND_URL=http://localhost:8080
WEB_PORT=8080